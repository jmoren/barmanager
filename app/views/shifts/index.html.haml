%h1 Listing shifts

%table
  %tr
    %th Apertura
    %th Cierre
    %th Caja inicial
    %th
    %th

  - @shifts.each do |shift|
    %tr
      %td= shift.open
      %td= shift.close
      %td= shift.money
      %td= link_to 'Ver', shift
      - if shift.is_open?
        %td= link_to 'Cerrar', "shifts/#{shift.id}/close", method: :post
      - else
        %td= ""

%br

= link_to 'New Shift', new_shift_path
